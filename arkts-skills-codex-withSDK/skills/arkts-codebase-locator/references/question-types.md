# HarmonyOS功能定位类问题类型详解

## 问题类型分类体系

本文件详细描述HarmonyOS ArkTS应用开发中常见的8大类功能定位问题类型，为技能识别和分类提供标准化依据。

## 1. 页面和导航功能定位

### 问题特征
- **关键词**：页面、界面、屏幕、路由、导航、跳转、返回
- **语义**：询问UI界面或导航逻辑的实现位置

### 典型问题模式
- **页面实现**：
  - "登录页面在哪实现？"
  - "首页界面在哪定义？"
  - "用户设置页面在哪？"
  - "关于页面在哪？"

- **导航配置**：
  - "路由配置在哪？"
  - "导航逻辑在哪实现？"
  - "页面跳转在哪定义？"
  - "返回功能在哪？"

- **组件关联**：
  - "页面组件在哪？"
  - "布局文件在哪？"
  - "模板文件在哪？"

### 核心文件类型
- `*.ets` - 页面组件文件
- `router` - 导航配置
- `navigation` - 导航组件
- `AppStage.ets` - 应用入口

### 搜索策略
1. 搜索`@Entry`装饰器标识的页面文件
2. 查找`router.push()`, `router.back()`等导航调用
3. 识别`Navigation`组件使用位置

## 2. 数据存储和状态管理定位

### 问题特征
- **关键词**：数据、存储、状态、缓存、持久化、配置、偏好
- **语义**：询问数据管理和存储逻辑的实现位置

### 典型问题模式
- **数据存储**：
  - "用户数据存储在哪？"
  - "本地存储在哪实现？"
  - "缓存逻辑在哪？"
  - "数据持久化在哪？"

- **状态管理**：
  - "全局状态管理在哪实现？"
  - "状态更新逻辑在哪？"
  - "数据同步在哪？"
  - "状态监听在哪？"

- **配置管理**：
  - "应用配置在哪？"
  - "用户偏好设置在哪？"
  - "配置文件在哪？"

### 核心文件类型
- `AppStorage` - 全局状态管理
- `Preferences` - 偏好设置
- `RelationalStore` - 关系数据库
- `stores/` - 状态管理目录
- `models/` - 数据模型目录

### 搜索策略
1. 搜索`@State`, `@Observed`, `@Prop`等状态装饰器
2. 查找`AppStorage.create()`或`Preferences.get()`调用
3. 识别数据模型定义和接口

## 3. UI组件和交互定位

### 问题特征
- **关键词**：组件、按钮、输入框、弹窗、对话框、布局、样式、交互
- **语义**：询问UI元素和交互逻辑的实现位置

### 典型问题模式
- **基础组件**：
  - "按钮组件在哪实现？"
  - "输入框组件在哪？"
  - "列表组件在哪定义？"
  - "文本组件在哪？"

- **交互逻辑**：
  - "点击事件在哪处理？"
  - "弹窗逻辑在哪？"
  - "表单验证在哪？"
  - "交互反馈在哪？"

- **布局样式**：
  - "布局文件在哪？"
  - "样式定义在哪？"
  - "主题配置在哪？"
  - "样式文件在哪？"

### 核心文件类型
- `components/` - 组件目录
- `common/` - 公共组件
- `*.ets` - 组件文件
- `styles/` - 样式目录
- `themes/` - 主题目录

### 搜索策略
1. 搜索`@Component`装饰器标识的组件文件
2. 查找`onClick`, `onAppear`等事件处理函数
3. 识别样式文件和布局配置

## 4. API和网络请求定位

### 问题特征
- **关键词**：API、接口、网络、请求、HTTP、服务、端点
- **语义**：询问API调用和网络相关功能的实现位置

### 典型问题模式
- **API调用**：
  - "登录接口在哪调用？"
  - "网络请求逻辑在哪实现？"
  - "API调用在哪？"
  - "数据请求在哪？"

- **接口定义**：
  - "API接口定义在哪？"
  - "接口文件在哪？"
  - "类型定义在哪？"
  - "接口文档在哪？"

- **服务集成**：
  - "第三方服务集成在哪？"
  - "服务配置在哪？"
  - "认证逻辑在哪？"
  - "token管理在哪？"

### 核心文件类型
- `api/` - API接口目录
- `services/` - 服务目录
- `network/` - 网络目录
- `utils/` - 工具目录
- `constants/` - 常量目录

### 搜索策略
1. 搜索`http`, `axios`, `fetch`等HTTP库调用
2. 查找API接口定义和类型声明
3. 识别网络请求配置和错误处理

## 5. 业务逻辑实现定位

### 问题特征
- **关键词**：逻辑、算法、计算、处理、业务、规则、流程
- **语义**：询问核心业务逻辑和算法功能的实现位置

### 典型问题模式
- **核心算法**：
  - "价格计算逻辑在哪？"
  - "核心算法在哪实现？"
  - "数据处理在哪？"
  - "计算逻辑在哪？"

- **业务规则**：
  - "业务规则在哪定义？"
  - "验证逻辑在哪？"
  - "权限检查在哪？"
  - "业务流程在哪？"

- **数据处理**：
  - "数据转换在哪？"
  - "格式化逻辑在哪？"
  - "过滤逻辑在哪？"
  - "排序逻辑在哪？"

### 核心文件类型
- `business/` - 业务逻辑目录
- `logic/` - 逻辑处理目录
- `algorithms/` - 算法目录
- `rules/` - 规则目录
- `utils/` - 工具函数目录

### 搜索策略
1. 搜索核心业务逻辑函数和类
2. 查找算法实现和计算函数
3. 识别业务规则验证逻辑

## 6. 配置和常量定位

### 问题特征
- **关键词**：配置、常量、枚举、参数、设置、选项
- **语义**：询问配置文件和常量定义的实现位置

### 典型问题模式
- **配置文件**：
  - "API配置文件在哪？"
  - "环境配置在哪？"
  - "应用配置在哪？"
  - "服务配置在哪？"

- **常量定义**：
  - "常量定义在哪？"
  - "全局变量在哪？"
  - "参数配置在哪？"
  - "默认值在哪？"

- **枚举定义**：
  - "枚举值在哪定义？"
  - "类型常量在哪？"
  - "状态码在哪？"
  - "选项配置在哪？"

### 核心文件类型
- `config/` - 配置目录
- `constants/` - 常量目录
- `enums/` - 枚举目录
- `settings/` - 设置目录
- `env/` - 环境目录

### 搜索策略
1. 搜索`const`, `let`全局常量定义
2. 查找`enum`类型定义
3. 识别配置文件和环境变量

## 7. 事件和生命周期定位

### 问题特征
- **关键词**：事件、生命周期、初始化、创建、显示、销毁、回调
- **语义**：询问事件处理和生命周期的实现位置

### 典型问题模式
- **生命周期**：
  - "生命周期函数在哪？"
  - "初始化逻辑在哪实现？"
  - "创建方法在哪？"
  - "销毁逻辑在哪？"

- **事件处理**：
  - "点击事件在哪处理？"
  - "事件监听在哪？"
  - "回调函数在哪？"
  - "事件绑定在哪？"

- **状态变化**：
  - "状态变化处理在哪？"
  - "监听器在哪？"
  - "通知机制在哪？"
  - "响应逻辑在哪？"

### 核心文件类型
- `lifecycle/` - 生命周期目录
- `events/` - 事件目录
- `listeners/` - 监听器目录
- `*.ets` - 组件生命周期方法

### 搜索策略
1. 搜索`onCreate()`, `onShow()`, `onDestroy()`等生命周期方法
2. 查找事件监听器和回调函数
3. 识别状态变化处理逻辑

## 8. 权限和安全定位

### 问题特征
- **关键词**：权限、安全、验证、认证、检查、加密、授权
- **语义**：询问权限和安全相关功能的实现位置

### 典型问题模式
- **权限申请**：
  - "权限申请在哪实现？"
  - "权限检查在哪？"
  - "授权逻辑在哪？"
  - "权限配置在哪？"

- **安全验证**：
  - "身份验证逻辑在哪？"
  - "安全检查在哪？"
  - "验证逻辑在哪？"
  - "认证在哪？"

- **数据处理**：
  - "加密逻辑在哪？"
  - "解密在哪？"
  - "安全处理在哪？"
  - "保护机制在哪？"

### 核心文件类型
- `security/` - 安全目录
- `permissions/` - 权限目录
- `auth/` - 认证目录
- `validation/` - 验证目录
- `utils/` - 安全工具目录

### 搜索策略
1. 搜索`requestPermissions()`等权限申请调用
2. 查找身份验证和安全检查逻辑
3. 识别加密和安全处理函数

## 问题类型识别指南

### 关键词匹配表
| 问题类型 | 关键词 | 语义特征 |
|---------|--------|----------|
| 页面导航 | 页面、界面、路由、导航、跳转、返回 | UI界面或导航路径 |
| 数据存储 | 数据、存储、状态、缓存、持久化、配置 | 数据管理或存储 |
| UI组件 | 组件、按钮、输入框、弹窗、布局、样式 | UI元素或交互 |
| API网络 | API、接口、网络、请求、HTTP、服务 | 网络通信或服务 |
| 业务逻辑 | 逻辑、算法、计算、处理、业务、规则 | 核心业务或算法 |
| 配置常量 | 配置、常量、枚举、参数、设置、选项 | 配置或常量定义 |
| 事件生命周期 | 事件、生命周期、初始化、创建、销毁、回调 | 事件或状态管理 |
| 权限安全 | 权限、安全、验证、认证、检查、加密 | 安全或权限控制 |

### 问题分类流程
1. **提取关键词**：从问题中提取相关关键词
2. **语义分析**：分析问题的语义和意图
3. **匹配类型**：匹配到8大类问题类型中的一类
4. **确定方向**：确定搜索和定位的具体方向

### 特殊情况处理
- **混合问题**：当问题涉及多个类型时，选择主要类型作为主导，其他类型作为补充
- **模糊问题**：当问题不够具体时，要求用户提供更多信息
- **新功能问题**：当询问新功能实现时，基于现有架构推断可能位置

## 辅助工具

### 问题类型检测函数
```typescript
function detectQuestionType(question: string): string {
  // 实现问题类型检测逻辑
  // 返回问题类型代码：1-8
}
```

### 关键词权重分析
- **高权重关键词**：直接对应问题类型
- **中权重关键词**：可能关联多个问题类型
- **低权重关键词**：需要结合上下文判断

### 语义相似度计算
- 使用关键词匹配计算语义相似度
- 结合上下文理解问题真实意图
- 处理歧义和模糊表达
# Kit 任务场景覆盖

## arkui-development

- 组件内状态迁移。
- 父子组件状态同步迁移。
- 跨层级状态共享迁移。
- 页面刷新不触发、状态不同步、渲染时机异常排障。

## arkdata-development

- 本地持久化状态迁移。
- 用户偏好和配置状态迁移。
- 需要跨页面保留的轻量状态迁移。

详细用法、状态规则和持久化能力边界读取对应 Kit 的 `routing.md`，不要把细节清单放在触发层。

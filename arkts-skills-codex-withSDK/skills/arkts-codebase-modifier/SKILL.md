---
name: arkts-codebase-modifier
description: 针对ArkTS语言的Harmony应用代码仓功能修改类问题回答。专注于"给xx增加开关/参数/流程怎么做？"、"新增视频播放怎么实现？"等功能修改类问题。提供基于代码仓事实的功能修改建议，不直接修改代码。使用场景包括：(1) 功能新增实现，(2) 参数/开关添加，(3) 流程修改优化，(4) 组件功能扩展，(5) 交互逻辑调整，(6) 多媒体功能集成。依赖arkts-locator提供的定位信息。
---

# ArkTS 功能修改问答技能

## 概述

本技能专注于ArkTS语言的Harmony应用代码仓功能修改类问题回答。当开发者询问"给xx增加开关/参数/流程怎么做？"、"新增视频播放怎么实现？"等功能修改问题时，本技能基于arkts-locator提供的定位信息，分析代码仓实际情况，给出具体的功能修改建议和实现方案。本技能仅提供问答和建议，不直接修改代码。

## 功能修改类问题类型

本技能支持以下9大类功能修改问题：

### 1. UI组件功能扩展
**问题特征**：询问UI组件的新功能、交互、样式修改
**典型问题**：
- "给列表项增加长按删除按钮"
- "给卡片组件增加展开/收起开关"
- "给按钮增加点击缩放动画"
- "给弹窗增加拖拽功能"
**修改建议位置**：组件装饰器、build方法、事件处理函数、样式配置

### 2. 状态与数据流增强
**问题特征**：询问状态变量、数据流、存储的扩展
**典型问题**：
- "给用户信息增加年龄字段"
- "给购物车增加多选状态"
- "给列表数据增加加载状态标识"
- "给缓存增加过期时间配置"
**修改建议位置**：@State/@Observed变量、数据模型、Storage/Preferences、状态管理器

### 3. 网络请求与API扩展
**问题特征**：询问API调用、网络请求的扩展
**典型问题**：
- "给登录接口增加图形验证码"
- "给接口调用增加重试机制"
- "给列表请求增加分页参数"
- "给数据接口增加加密传输"
**修改建议位置**：服务层、API封装、拦截器、请求配置、数据转换函数

### 4. 页面功能新增与增强
**问题特征**：询问页面新增、路由、导航功能
**典型问题**：
- "增加设置页面怎么做"
- "给详情页增加返回首页按钮"
- "给Tab导航增加未读消息标记"
- "给页面跳转增加参数传递"
**修改建议位置**：页面组件、路由配置、Navigation、页面参数、生命周期方法

### 5. 多媒体功能集成
**问题特征**：询问视频、音频、图片等多媒体功能
**典型问题**：
- "新增视频播放怎么实现"
- "给图片查看器增加缩放功能"
- "给音频播放器增加进度条"
- "增加相机拍照功能"
**修改建议位置**：媒体组件、AVPlayer、Image组件、Camera组件、媒体服务层

### 6. 交互逻辑与流程调整
**问题特征**：询问交互流程、业务逻辑的修改
**典型问题**：
- "给注册流程增加手机号验证"
- "给下单流程增加地址选择"
- "给搜索增加历史记录功能"
- "给表单提交增加二次确认"
**修改建议位置**：业务逻辑层、事件处理函数、流程控制代码、条件判断逻辑

### 7. 动画与特效增强
**问题特征**：询问动画效果、视觉特效的实现
**典型问题**：
- "给列表项增加进入动画"
- "给页面切换增加滑动过渡"
- "给按钮增加呼吸灯效果"
- "给加载状态增加骨架屏"
**修改建议位置**：animateTo方法、组件动画属性、transition属性、动画配置

### 8. 设备能力与硬件交互
**问题特征**：询问设备特性、硬件功能的集成
**典型问题**：
- "增加蓝牙连接功能"
- "增加定位服务"
- "增加NFC读写功能"
- "增加传感器数据采集"
**修改建议位置**：设备能力API、权限申请、硬件服务层、数据处理模块

### 9. 性能优化与体验改进
**问题特征**：询问性能优化、用户体验改进
**典型问题**：
- "给列表增加懒加载"
- "给图片增加缓存机制"
- "给长列表增加虚拟滚动"
- "给数据请求增加节流"
**修改建议位置**：组件性能配置、缓存策略、请求优化、渲染优化

## 工作流程

### 步骤1：功能修改问题类型识别

**目标**：识别用户问题的具体功能修改类型

**操作流程**：
1. **关键词分析**：
   - 提取问题中的动词（增加、新增、修改、优化、集成等）
   - 提取问题中的目标对象（组件、页面、接口、数据等）
   - 提取问题中的功能描述（开关、参数、流程、动画等）

2. **类型匹配**：
   - 根据关键词组合匹配到10大类问题类型
   - 确定问题的核心修改方向
   - 识别问题的复杂度（简单参数调整 vs 复杂功能集成）

3. **问题拆解**：
   - 对于复杂问题，拆解为多个子任务
   - 确定每个子任务的修改类型
   - 建立修改任务的优先级和依赖关系

**输出**：问题类型识别结果，修改任务清单，复杂度评估

### 步骤2：基于locator信息的代码定位

**目标**：利用arkts-locator传递的定位信息，了解修改涉及的代码范围

**操作流程**：
1. **加载locator信息**：
   - 获取arkts-locator提供的定位结果
   - 解析核心文件清单和关键入口点
   - 分析代码模块的依赖关系

2. **定位范围确认**：
   - 确认功能修改涉及的主要文件
   - 识别受影响的组件和模块
   - 分析修改的传播影响范围

3. **入口点识别**：
   - 确定功能的主要入口点
   - 识别关键的配置位置
   - 分析数据流转路径

**输出**：修改涉及的核心文件清单，关键入口点，影响范围分析

### 步骤3：基于DAG组合的代码读取

**目标**：根据问题类型和locator信息，选择对应的DAG组合读取代码

**操作流程**：

#### DAG选择策略
根据问题类型选择对应的DAG组合：

**UI组件功能扩展** → DAG_UICOMPONENT
```
locator[components] → component_definition → build_method → event_handlers → styles
```
1. 读取组件定义文件（@Component装饰器）
2. 分析build方法的UI结构
3. 查看事件处理函数（onClick、onAppear等）
4. 检查样式配置（@Styles、@Extend等）

**状态与数据流增强** → DAG_STATE
```
locator[stores] → state_variables → data_models → storage_services → data_flow
```
1. 读取状态管理文件（@State、@Observed等）
2. 分析数据模型定义
3. 查看存储服务实现
4. 追踪数据流转路径

**网络请求与API扩展** → DAG_NETWORK
```
locator[services] → api_definitions → http_client → interceptors → data_transformers
```
1. 读取API接口定义
2. 分析HTTP客户端配置
3. 查看拦截器实现
4. 检查数据转换逻辑

**页面功能新增与增强** → DAG_PAGE
```
locator[pages] → page_components → router_config → navigation → page_params
```
1. 读取页面组件定义
2. 分析路由配置文件
3. 查看Navigation组件使用
4. 检查页面参数传递机制

**多媒体功能集成** → DAG_MEDIA
```
locator[media] → media_components → player_services → media_utils → permissions
```
1. 读取媒体组件实现
2. 查看播放器服务
3. 检查媒体工具函数
4. 分析权限申请逻辑

**交互逻辑与流程调整** → DAG_INTERACTION
```
locator[business] → event_handlers → business_logic → flow_control → validation
```
1. 读取事件处理函数
2. 分析业务逻辑实现
3. 查看流程控制代码
4. 检查验证逻辑

**动画与特效增强** → DAG_ANIMATION
```
locator[components] → animation_configs → animate_methods → component_props → transitions
```
1. 读取动画配置
2. 查看animateTo方法调用
3. 检查组件动画属性
4. 分析transition定义

**设备能力与硬件交互** → DAG_HARDWARE
```
locator[services] → device_apis → permission_manager → data_processor → event_listeners
```
1. 读取设备API调用
2. 查看权限管理器
3. 检查数据处理模块
4. 分析事件监听器

**性能优化与体验改进** → DAG_PERFORMANCE
```
locator[components] → performance_configs → cache_strategies → lazy_loading → render_optimizations
```
1. 读取性能配置
2. 查看缓存策略
3. 检查懒加载实现
4. 分析渲染优化代码

**输出**：代码读取结果，关键代码片段，文件重要性评估

### 步骤4：功能修改方案设计

**目标**：基于代码分析，给出符合SOLID原则的修改方案

**操作流程**：
1. **SOLID原则应用**：
   - **单一职责**：确保修改点职责单一，不引入过多功能
   - **开闭原则**：优先通过扩展而非修改实现新功能
   - **里氏替换**：确保继承关系的一致性
   - **接口隔离**：避免不必要的依赖接口
   - **依赖倒置**：依赖抽象而非具体实现

2. **修改位置判断**：
   - **最佳修改位置**：根据代码结构和SOLID原则确定最合适的修改点
   - **扩展方式选择**：选择装饰器、组合、继承等合适的扩展方式
   - **配置化设计**：对于开关、参数等，考虑配置化管理

3. **实现方案设计**：
   - **代码结构设计**：设计新增代码的组织结构
   - **接口定义**：定义必要的接口和类型
   - **数据流设计**：设计数据流转和状态更新机制
   - **错误处理**：设计异常处理和边界情况处理

4. **影响范围评估**：
   - **前向影响**：评估修改对下游代码的影响
   - **后向影响**：评估修改对上游依赖的影响
   - **横向影响**：评估修改对同层模块的影响

**输出**：修改方案设计，最佳修改位置，实现建议，影响范围评估

### 步骤5：回退机制和重试策略

**目标**：在无法回答或出错时提供备选方案

**错误类型**：

#### 1. 定位信息不足
- **表现**：locator提供的信息不足以确定修改位置
- **处理**：
  1. 回退到步骤2，重新分析locator信息
  2. 尝试使用repomap自行定位相关模块
  3. 向用户请求更多上下文信息

#### 2. 代码理解偏差
- **表现**：代码结构理解不准确，导致修改方案不可行
- **处理**：
  1. 回退到步骤3，重新读取相关代码
  2. 选择不同的DAG组合探索代码
  3. 扩展代码读取范围

#### 3. 修改方案冲突
- **表现**：修改方案与现有代码结构冲突
- **处理**：
  1. 回退到步骤4，重新设计修改方案
  2. 考虑不同的扩展方式
  3. 评估是否需要重构现有代码

#### 4. 信息不完整
- **表现**：代码仓中缺少必要的实现或配置
- **处理**：
  1. 承认信息不足，不进行假设
  2. 向用户明确说明缺失的部分
  3. 请求用户提供更多信息或确认需求

**回退机制**：
1. **步骤级回退**：当前步骤失败时，返回上一步重新尝试
2. **DAG路径回退**：当前DAG探索失败时，尝试其他DAG路径
3. **降级策略**：当三次重试失败后，使用repomap重新定位

**重试策略**：
1. **三次重试机制**：每个步骤最多重试3次
2. **备选方案**：每次重试采用不同的策略
3. **人工干预**：重试失败后，向用户请求澄清

### 步骤6：结构化回答生成

**目标**：提供清晰、可操作的功能修改建议

**回答模板结构**：

```
# [功能名称] 修改方案

## 🎯 修改目标
[简要说明要实现的功能修改目标]

## 📋 问题类型识别
- **问题类型**：[识别的问题类型]
- **复杂度**：[简单/中等/复杂]
- **修改范围**：[涉及的代码模块]

## 🔍 代码定位分析
基于locator提供的定位信息：

### 核心文件清单
- **主要文件**：`文件路径:行号` - 功能描述
- **辅助文件**：`文件路径:行号` - 功能描述
- **配置文件**：`文件路径:行号` - 功能描述

### 关键入口点
- **入口1**：`文件路径:函数名` - 说明
- **入口2**：`文件路径:函数名` - 说明

### 代码探索路径
1. 探索步骤1（基于DAG路径）
2. 探索步骤2（关键发现）
3. 探索步骤3（最终定位）

## 💡 修改方案设计

### SOLID原则应用
- **单一职责**：说明如何确保职责单一
- **开闭原则**：说明如何通过扩展实现
- **其他原则**：[根据情况说明]

### 最佳修改位置
**推荐位置1**：`文件路径:行号`
- **理由**：说明为什么选择这个位置
- **修改方式**：具体修改建议

**推荐位置2**（备选）：`文件路径:行号`
- **理由**：备选方案的理由
- **修改方式**：具体修改建议

### 实现建议

#### 代码结构
// 示例代码结构
[提供核心代码示例]

#### 数据流设计
- **状态变量**：需要新增或修改的状态变量
- **数据流转**：数据如何流转和更新
- **事件处理**：如何处理用户交互

#### 配置化设计（如适用）
// 配置示例
const config = {
  // 配置项
};

## 📝 关键代码示例

### 文件1：`文件路径`
// 核心修改位置
[展示关键代码片段，说明需要修改的位置]

### 文件2：`文件路径`
// 相关修改位置
[展示相关代码片段]


## ⚠️ 注意事项
- **依赖影响**：说明修改可能影响的依赖
- **兼容性**：说明兼容性考虑
- **性能影响**：说明性能方面的考虑
- **测试建议**：建议测试的场景和用例

## 🔄 替代方案
[如果存在多种实现方式，提供替代方案]

## ❓ 需要确认的事项
[如果有信息不足或需要用户确认的地方，明确列出]
```

### 步骤7：事实核查与保证

**目标**：确保所有回答都基于真实的代码仓内容

**核查原则**：
1. **代码验证**：每个提到的文件、函数、变量都必须在代码中实际存在
2. **行号验证**：确认提供的行号引用准确无误
3. **代码片段验证**：确保展示的代码片段真实可用
4. **逻辑验证**：确保修改方案的逻辑一致性

**信息不足处理**：
1. **承认未知**：明确告知无法确定的部分，不进行猜测
2. **反问澄清**：列出需要用户澄清的问题
3. **提供条件方案**：如果存在多种可能，提供不同条件下的方案

**文档记录**：
- 记录完整的分析过程和探索路径
- 保存关键发现和代码片段
- 记录locator信息和repomap使用情况
- 维护修改方案的设计决策

**禁止事项**：
- 禁止臆测代码中不存在的实现
- 禁止假设未明确的需求细节
- 禁止提供未经验证的代码示例
- 禁止使用edit工具修改代码

## 输出标准

### 忠实性要求
- 一切结果都必须源于真实的代码仓内容
- 引用的代码片段必须准确可查
- 信息不足时明确告知，不进行假设

### 准确性要求
- 文件路径、行号必须准确无误
- 代码示例必须真实存在
- 修改建议必须基于实际代码结构

### 完整性要求
- 提供完整的修改方案
- 给出关键代码示例
- 标注注意事项和影响范围

### 可操作性要求
- 提供具体的修改位置
- 给出清晰的实现步骤
- 提供可参考的代码示例

## 常见问题处理

### 问题1：修改点不明确
**处理**：
1. 分析代码结构，提供多个可能的修改位置
2. 比较各位置的优缺点
3. 推荐最佳位置并说明理由

### 问题2：功能过于复杂
**处理**：
1. 将复杂功能拆解为多个子任务
2. 为每个子任务提供独立的修改方案
3. 说明子任务间的依赖关系

### 问题3：现有代码不支持
**处理**：
1. 明确说明现有代码的局限性
2. 提供必要的重构建议
3. 说明重构的影响范围和风险评估

### 问题4：多种实现方式
**处理**：
1. 提供多种实现方案
2. 比较各方案的优缺点
3. 推荐最佳方案并说明理由

### 问题5：locator信息不足
**处理**：
1. 尝试使用repomap自行定位
2. 扩展搜索范围
3. 向用户请求更多上下文信息

## 使用示例

**用户问题**："给列表项增加长按删除功能怎么做？"

**技能执行流程**：

**步骤1：问题类型识别**
- 识别为"UI组件功能扩展"类型
- 问题关键词：列表项、长按、删除功能
- 复杂度：中等（涉及事件处理、UI更新、数据修改）

**步骤2：基于locator信息定位**
- locator提供的核心文件：`entry/src/main/ets/pages/ItemList.ets`
- 关键入口点：`List`组件、`ListItem`组件、事件处理函数
- 影响范围：列表页面、数据管理模块

**步骤3：基于DAG_UICOMPONENT读取代码**
```
读取ItemList.ets → 分析build方法 → 查看ListItem定义 → 检查事件处理
```
发现代码结构：
- 使用`List`和`ListItem`组件
- 当前只有`onClick`事件
- 数据通过`@State`管理

**步骤4：修改方案设计**
- **最佳修改位置**：`ItemList.ets:build方法中的ListItem组件`
- **SOLID原则**：开闭原则，通过扩展事件处理而非修改组件
- **实现方式**：
  1. 在`ListItem`上添加`onLongPress`事件
  2. 新增删除函数`deleteItem(index: number)`
  3. 更新状态中的数据
  4. 添加删除确认对话框

**步骤5：生成结构化回答**
提供完整的修改方案，包括：
- 修改位置和理由
- 代码示例
- 实现步骤
- 注意事项

## 参考文档

本技能依赖以下参考资料：

- [SOLID原则应用指南](references/solid-principles.md) - SOLID原则在ArkTS中的应用

通过这种系统化的方法，结合arkts-locator提供的定位信息和DAG探索模式，本技能能够为HarmonyOS ArkTS应用的功能修改提供准确、可操作的问答建议，帮助开发者高效完成功能增强和新功能集成。
